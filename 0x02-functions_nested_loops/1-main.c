#include "main.h"

/**
 * main - entry point
 * print_alphabet - prints the alphabet in lowercase followed by a new line
 *
 * Return: Always 0.
 */
void print_alphabet(void);
int main(void)
{
    print_alphabet();
    return (0);
}
